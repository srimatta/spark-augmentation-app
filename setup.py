from setuptools import setup

setup(name='SparkAugmentationApp',
      version='1.0',
      description='PySpark application for Image Augmentation.',
      packages=['app'],
      py_moudles=['app'],
      zip_safe=False)